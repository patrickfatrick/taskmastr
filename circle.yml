machine:
  node:
    version: 5
dependencies:
  pre:
    - npm cache clear
    - npm install npm@'>=3.7.2' -g
test:
  post:
    - cat ./coverage/**/lcov.info | ./node_modules/codecov/bin/codecov
general:
  artifacts:
    - ./coverage
deployment:
  production:
    branch: master
    commands: >
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - git push git@heroku.com:taskmastr-app.git ${CIRCLE_SHA1}:refs/heads/master
      - curl -X DELETE "https://api.cloudflare.com/v4/zones/${CLOUDFLARE_ZONE}/purge_cache"
        -H "X-Auth-Email: ${CLOUDFLARE_EMAIL}"
        -H "X-Auth-Key: ${CLOUDFLARE_AUTH}"
        -H "Content-Type: application/json"
        --data '{"files":["https://www.taskmastr.co/dist/bundle.js","https://www.taskmastr.co/dist/stylesheets/styles.css"]}'
